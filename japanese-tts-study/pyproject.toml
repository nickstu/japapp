[project]
name = "japanese-tts-study"
version = "0.1.0"
description = "Local GUI for generating Japanese study dialogue audio with Gemini multi-speaker TTS."
requires-python = ">=3.11"
dependencies = [
    "lameenc>=1.8.1",
    "pygame>=2.6.1",
]

[tool.uv]
package = false
